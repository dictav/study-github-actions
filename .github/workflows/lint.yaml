name: Failures

on:
  push:

jobs:
  run-linters:
    runs-on: ubuntu-latest

    steps:

      - run: echo "🍻"

      - run: echo "1. previous step failed"
        if: failure()

      - run: exit 1

      - run: echo "2. previous step failed"
        if: failure()
